# Production (https://tether.bushaan.dev)
# Used automatically by `pnpm build` / Docker build (Vite mode=production)
#
# Same-origin style — assumes a reverse proxy serves the UI and
# forwards /api + /socket.io to the backend on this domain.
VITE_API_URL=https://tether.bushaan.dev/api
VITE_SOCKET_URL=https://tether.bushaan.dev
